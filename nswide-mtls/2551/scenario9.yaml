# Invalid: there isn't any Destination Rule enabling services start mTLS connection | VALID with AutoMTLS
apiVersion: "security.istio.io/v1beta1"
kind: "PeerAuthentication"
metadata:
  name: "default"
  namespace: bookinfo
spec:
  mtls:
    mode: STRICT
